export enum AppRoutes {
  Today = 'today',
  Tasks = 'tasks',
  TaskDetail = 'task',
  Goals = 'goals',
  Habits = 'habits'
}

export enum AppRouterLinks {
  Today = '/today',
  Tasks = '/tasks',
  TaskDetail = '/task',
  Goals = '/goals',
  Habits = '/habits'
}