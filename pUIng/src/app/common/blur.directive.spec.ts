import { BlurDirective } from './blur.directive';

describe('BlurDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurDirective();
    expect(directive).toBeTruthy();
  });
});
