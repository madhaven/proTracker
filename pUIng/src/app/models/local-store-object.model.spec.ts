import { LocalStoreObject } from './local-store-object.model';

describe('LocalStoreObject', () => {
  it('should create an instance', () => {
    expect(new LocalStoreObject()).toBeTruthy();
  });
});
